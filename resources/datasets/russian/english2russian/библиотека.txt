библиот`е`ка
++++++++++
1. [`noun, feminine`] library
· В больших городах `библиотека` обычно состоит из многих филиалов. (In big cities, a `library` usually consists of many branches.)
· Я люблю читать книги из `библиотеки`. (I love to read books from a `library`.)
2. [`verb`] library
· Они `библиотекировали` большую коллекцию книг. (They `libraryed` a big collection of books.)
· Они `библиотекировали` книги по теме юриспруденции. (They `libraryed` books on the topic of jurisprudence.)