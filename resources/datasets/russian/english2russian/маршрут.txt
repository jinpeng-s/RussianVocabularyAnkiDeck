маршр`у`т
++++++++++
1. [`noun, masculine`] route
· Я следую моему `маршруту`. (I am following my `route`.)
· Он предложил нам короткий `маршрут` к морю. (He suggested a short `route` to the sea.)
2. [`verb`] route
· Поезд был `маршрутирован` в другую станцию. (The train was `routed` to a different station.)
· Транспорт будет `маршрутироваться` через этот район. (The transport will be `routed` through this district.)