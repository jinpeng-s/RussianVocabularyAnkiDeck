шкал`а`
++++++++++
1. [`noun, feminine`] scale
· В данной `шкале` представлено пять уровней. (There are five levels in this `scale`.)
· Для оценки  используется `шкала` от 0 до 10. (A `scale` of 0-10 is used for grading.)
2. [`verb, infinitive`] scale
· Мы `шкалируем` сложность для каждого задания. (We `scale` the difficulty of each task.)
· Вы можете `шкалировать` размеры файлов в произвольных единицах. (You can `scale` the file sizes in arbitrary units.)